#### Rota de customers

- `Post:` /customers

```json
// payload
  {
    "name": "Company fullname ltda",
    "document": "cnpj or cpf"
  }
```

- `Put:` /customers/:id

```json
// payload
  {
    "name": "Company fullname ltda",
    "document": "cnpj or cpf"
  }
```

- `Get:` /customers
- `Get:` /customers/:id
